name: MLB Position Player Pitching Alert

on:
  workflow_dispatch:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  alert:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Restore alert state cache
        uses: actions/cache/restore@v5
        with:
          path: .cache
          key: position-player-alert-${{ github.run_id }}
          restore-keys: |
            position-player-alert-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ALERT_ONLY_LATE_INNINGS: 'true'
          LATE_INNING_THRESHOLD: '7'
          SCORE_DIFF_THRESHOLD: '6'
          ENABLE_BLOWOUT_WARNING: 'true'
          INCLUDE_BLOWOUT_ONLY: 'true'
          CONFIRMED_ALERT_REPEAT_COUNT: '3'
          CONFIRMED_ALERT_REPEAT_DELAY_SECONDS: '3'
          FINAL_GAME_RECHECK_LOOKBACK_HOURS: '48'
          SEASON_CASE_CACHE_TTL_SECONDS: '21600'
          STATE_FILE: .cache/position_player_alert_state.json
        run: |
          mkdir -p .cache
          python scheduled_run.py

      - name: Save alert state cache
        if: always()
        uses: actions/cache/save@v5
        with:
          path: .cache
          key: position-player-alert-${{ github.run_id }}
